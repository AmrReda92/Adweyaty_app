import 'package:flutter/material.dart';

class VitaminScreen extends StatelessWidget {
  const VitaminScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle:true ,
        title: Text("Vitamin Medicines") ,
      ),

    );
  }
}
