import 'package:flutter/material.dart';

class WomanProductsScreen extends StatelessWidget {
  const WomanProductsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle:true ,
        title: Text("woman Medicines and skin care") ,
      ),

    );
  }
}
