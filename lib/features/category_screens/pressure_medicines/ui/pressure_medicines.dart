import 'package:flutter/material.dart';

class PressureMedicines extends StatelessWidget {
  const PressureMedicines({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
